          echo check java version?
          java -version
          echo check maven?
          mvn --version
          echo git install?
          git --version
          echo gradle and ant install?
          gradle --version
          ant -version
          echo where is Android sdk root?
          echo $ANDROID_SDK_ROOT  
          echo selelium jar path?
          echo $SELENIUM_JAR_PATH
          echo workspace location?
          echo $RUNNER_WORKSPACE
          echo who is running the script?
          whoami
          echo disk layout?
          df
          echo environment variable?
          env
